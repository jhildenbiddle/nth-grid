{{#if options}}
:root {
    {{!-- Global Options --}}
    {{#each options}}
    --nth-grid-{{@key}}: {{this}};
    {{/each}}
    --nth-grid-remove-globals: true;
}
{{/if}}

{{!-- Styles --}}
{{> grid.pcss parentSelector=".postcss"}}
